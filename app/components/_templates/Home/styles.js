import styled from 'styled-components';
import { Col as _Col } from 'react-styled-flexboxgrid';

export const Title = styled.h1`
	color: black;
	text-align: center;
`;

export const Col = styled(_Col)`
	padding: 0.5em;
`;

export const Home = Component => styled(Component)`
	// to do
`;

export default Home;
