import styled from 'styled-components';
import Greeting from './Greeting';

export default styled(Greeting)`
	background: white;
	box-shadow: 1px 1px 2px rgba(0, 0, 0, 0.5);
	height: 200px;
	padding: 10px;
`;
