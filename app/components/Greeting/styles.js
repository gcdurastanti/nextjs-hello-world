import styled from 'styled-components';

export const Greeting = Component => styled(Component)`
	background: white;
	box-shadow: 1px 1px 2px rgba(0, 0, 0, 0.5);
	height: 200px;
	padding: 10px;
`;

export default Greeting;
